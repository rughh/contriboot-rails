<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Contriboot Rails" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Contriboot Rails" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body data-logged-in-as="<%= current_user.try(:auth_id) %>">
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
    <footer class="page-footer">
      <div class="page-footer--inner">
        <p>Organized by <a href="https://www.bootev.org/">BOOT e.V.</a> (<a href="http://rubyunconf.eu/imprint.html">Imprint/Impressum</a>)</p>
        <p><a href="https://depfu.com/">Hosting sponsored by</a></p>
        <p><a href="https://depfu.com/"><%= image_tag 'depfu-left-white.png', alt: 'Depfu', class: 'depfu-logo' %></a></p>
        <p><%= link_to '¯\_(ツ)_/¯', print_path(format: :pdf) %></p>
      </div>
    </footer>
  </body>
</html>
